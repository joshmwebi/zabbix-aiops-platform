packages:
  # Provides the unique_combination_of_columns and accepted_range tests used
  # in the schema files. Install with: dbt deps
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
