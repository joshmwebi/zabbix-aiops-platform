# Core — Zabbix API access and configuration
requests>=2.32
python-dotenv>=1.0

# Alert enrichment
anthropic>=0.40
fastapi>=0.115
uvicorn>=0.32
pydantic>=2.9

# Pipeline — install the adapter for the backend you use
psycopg2-binary>=2.9 ; platform_system != "nothing"
snowflake-connector-python>=3.12
